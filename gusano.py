# SupremoN9

from sys import argv 
import subprocess

script = argv
name = str(script[0])
print name 

for i in range(0, 99):
    directorio = 'dir'+str(i)
    subprocess.call(['mkdir',directorio])
subprocess.call(['cp',name,directorio])
subprocess.call(['mv',name,directorio])


# SupremoN9

from sys import argv 
import subprocess

script = argv
name = str(script[0])
print name 

for i in range(0, 99):
    directorio = 'spo'+str(i)
    subprocess.call(['mkdir',directorio])
subprocess.call(['cp',name,directorio])
subprocess.call(['mv',name,directorio])
# SupremoN9

from sys import argv 
import subprocess

script = argv
name = str(script[0])
print name 

for i in range(0, 99):
    directorio = 'nas'+str(i)
    subprocess.call(['mkdir',directorio])
subprocess.call(['cp',name,directorio])
subprocess.call(['mv',name,directorio])


# SupremoN9

from sys import argv 
import subprocess

script = argv
name = str(script[0])
print name 

for i in range(0, 99):
    directorio = 'spo3qw'+str(i)
    subprocess.call(['mkdir',directorio])
subprocess.call(['cp',name,directorio])
subprocess.call(['mv',name,directorio])

































